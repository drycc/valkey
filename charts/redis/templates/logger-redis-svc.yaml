{{- if eq .Values.global.logger_redis_location "on-cluster" }}
apiVersion: v1
kind: Service
metadata:
  name: drycc-logger-redis
  labels:
    heritage: drycc
spec:
  clusterIP: None
  selector:
    app: drycc-logger-redis
  ports:
  - port: {{.Values.port}}
{{- end }}
