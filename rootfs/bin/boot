#!/usr/bin/env bash

set -eof pipefail

VALKEY_CONFIG_FILE=/etc/valkey/valkey.conf

# Set password
if [ -n "$DRYCC_VALKEY_PASSWORD" ]; then
    printf "\nrequirepass %s" "$DRYCC_VALKEY_PASSWORD" >> $VALKEY_CONFIG_FILE
fi

exec valkey-server $VALKEY_CONFIG_FILE "$@"
